import { Product } from "@/components/component/product";

export default function Main() {
    return (
        <Product
            id="nophy"
            version="1.0.0"
            title="Nophy"
            description="Mobile xigncode bypasser using frida and adb APIs. Also provides some frida tools."
            steps={[]}
            faqs={[]}
        />
    )
}